"""Fixture module exercising the Python analysis path."""


def divide(numerator: float, denominator: float = 1.0) -> float:
    """Divide one number by another.

    Args:
        numerator: The number being divided.
        denominator: The number to divide by. Defaults to 1.0.

    Returns:
        The quotient.
    """
    return numerator / denominator
